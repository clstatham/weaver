component Foo {
    a: int
    b: int
}

@startup
system Setup {
    spawn(Foo {
        a = 5
        b = 10
    })
}

@update
system Test {
    var bar = 0
    query [var foo: Foo] ~ {
        bar += 5
        foo.a += bar
        print(foo.a)
    }
}
